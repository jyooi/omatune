{
  "name": "@omatune/device-database",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "generate": "bun scripts/generate-model-table.ts",
    "build": "bun run generate && bun build src/index.ts --outdir dist --target bun",
    "test": "bun test"
  }
}
